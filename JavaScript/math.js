// Math = built-in object that provides a collection of properties and methods

console.log(Math.PI);
console.log(Math.E);

let x = -3.99;
let y = 2;
let z;

//z = Math.round(x); //will round to the nearest whole number
//z = Math.floor(x); //floor will always round down
//z = Math.ceil(x); //ceil will always round up
//z = Math.trunc(x); //truncate will remove any figures after a decimal
//z = Math.pow(x, y); //raises value to a given power
//z = Math.sqrt(x); //finds the square root
//z = Math.log(x); //finds natural logarithm of a number
//z = Math.sin(x); //find sine
//z = Math.cos(x); // find cosine
//z = Math.tan(x); // find tangent
//z = Math.abs(x); //finds the absolute value, essentially returns negative numbers as postive ones. it's the absolute distance from 0
//z = Math.sign(x); //if it's a negative number it's -1 if it's positive it's 1 if it's 0 it's 0
let max = Math.max(1,2,3,4);
let min = Math.min(1,2,3,4);


console.log(z);
console.log(max);
console.log(min);